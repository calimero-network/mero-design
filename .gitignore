# Rust / WASM
logic/target/
# `cargo mero build` output (wasm + emitted ABI/state schema)
logic/res/
# `cargo mero bundle` output (signed .mpk)
logic/dist/
*.wasm

# Node
app/node_modules/
app/dist/
app/dev-dist/
app/.env.local
app/.env.integration
app/e2e-report/
app/playwright-report/
app/test-results/

# macOS
.DS_Store

# Editor
.vscode/
.idea/
*.swp

# Logs
*.log
/tmp/*.log
/tmp/*.pid

# Calimero node homes (dev)
~/.calimero/merodesign-*

# build artifacts / caches (added 2026-06-08)
node_modules/
logic/res/*.mpk
logic/res/bundle-temp/
